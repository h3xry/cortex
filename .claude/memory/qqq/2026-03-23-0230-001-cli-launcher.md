# Quality Gate Report

**Feature:** 001-cli-launcher
**Date:** 2026-03-23
**Files Changed:** 25 new files

---

## Security Review
**Status:** FAIL

| Severity | Count |
|----------|-------|
| BLOCKER | 2 |
| HIGH | 3 |
| MEDIUM | 3 |
| LOW | 2 |

**BLOCKER-1:** Unrestricted path traversal in `/api/folders` — no root boundary check
**BLOCKER-2:** Wildcard CORS (`cors()`) with no origin restriction

**HIGH-1:** Unvalidated folderPath in session creation (same root cause as BLOCKER-1)
**HIGH-2:** Internal error messages leaked to client in 500 responses
**HIGH-3:** No authentication or origin check on WebSocket upgrade

---

## Code Review
**Status:** FAIL

| Severity | Count |
|----------|-------|
| BLOCKER | 3 |
| HIGH | 4 |
| MEDIUM | 6 |
| LOW | 6 |

**BLOCKER:** Same security issues as above (CORS, path traversal x2)
**HIGH-1:** Error classification by string matching (fragile)
**HIGH-2:** Async polling with no error handling in setInterval
**HIGH-3:** Unguarded JSON.parse on WebSocket message
**HIGH-4:** No WebSocket `error` event handler

Quality impact: Reliability [ISSUES] | Security [ISSUES] | Maintainability [ISSUES]

---

## Test Review
**Status:** FAIL
**Coverage:** ~45-50% (estimated, no coverage tool configured)

Missing test areas:
- `server/src/routes/sessions.ts` — 0% (no route test)
- `server/src/ws/terminal.ts` — 0% (no test)
- `client/src/**` — 0% (no tests)
- `session-manager.ts` — polling, listeners, getSession untested
- `tmux.ts` — capturePaneOutput untested
- `@vitest/coverage-v8` not installed

---

## Consistency Check
**Status:** HAS DEVIATIONS

| Severity | Count |
|----------|-------|
| Critical | 0 |
| Notable | 3 |
| Minor | 2 |

**NOTABLE-1:** `uuid` package declared but never used (uses `node:crypto` instead)
**NOTABLE-2:** Inconsistent error handling pattern between useFolders and useSessions hooks
**NOTABLE-3:** POST /sessions response omits `endedAt` field — type/runtime mismatch

---

## Dependency Audit
**Status:** WARN

| Category | Count |
|----------|-------|
| Vulnerabilities | 0 |
| Outdated | 9 (5 major) |
| Unused | 1 (`uuid`) |
| License issues | 0 |

**WARN-1:** `@types/express` v5 but `express` runtime is v4 — type mismatch
**WARN-2:** `uuid` package unused — remove

---

## Gate Decision

| Reviewer | Status |
|----------|--------|
| Security | ❌ FAIL (2 BLOCKER) |
| Code | ❌ FAIL (3 BLOCKER) |
| Test | ❌ FAIL (coverage ~45%) |
| Consistency | ⚠️ WARN (3 notable) |
| Dependencies | ⚠️ WARN (unused dep, type mismatch) |

### Result: **FAIL**

## Required Fixes

### Must Fix (blocks commit)
1. **Path traversal:** Add ALLOWED_ROOT boundary to `/api/folders` and session creation
2. **CORS:** Restrict to `http://localhost:5173`
3. **WebSocket origin check:** Validate Origin header on upgrade
4. **Error leaking:** Return generic message on 500, log raw error
5. **Polling error handling:** Add try/catch in setInterval async body
6. **WebSocket error handler:** Add `ws.on("error", ...)`
7. **JSON.parse guard:** Wrap in try/catch in useTerminal
8. **Typed errors:** Replace string matching with custom error classes
9. **Test coverage:** Add tests for sessions route, WebSocket handler, increase to 80%+
10. **Remove unused `uuid`** dependency

### Should Fix (recommended)
11. **POST /sessions response:** Include `endedAt` field
12. **WsMessage type:** Define on server side, use in sendMessage
13. **useSessions error state:** Add error field like useFolders
14. **@types/express:** Align with express v4
